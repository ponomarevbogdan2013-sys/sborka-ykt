# CHANGELOG — журнал изменений СБОРКА

Человекочитаемая история. Формат: дата · кто (дизайн-Claude / серверный / владелец) · что изменилось.
Дописывать после каждого заметного изменения. Читать при старте сессии вместе с CLAUDE.md.

## 2026-09-06

- серверный · кабинет партнёра: `src/routes_partner.js` — `/api/partner/login`·`logout`·`me`·`orders`·`payout`·`payouts` (сессия партнёра, cookie `sid`); `GET /qr/:code.png` — PNG-QR (`qrcode`) со ссылкой `?ref=<code>`, навесный цвет бренда; глобальный хук `onRequest` пишет переход по `?ref=` в `partner_clicks` и ставит cookie `ref` (атрибуция, 1 переход/сессия). Деньги считаются на лету: заработано = Σ `partner_commission_rub` по выполненным заказам с этим `ref`; баланс = заработано − выплачено − запрошено.
- серверный · админ создаёт партнёров: `POST /api/admin/partners` (basicAuth; `code` автогенерится транслитом из названия), `GET /api/admin/partners` (список со сводкой), `POST /api/admin/partners/payouts/:id` (`paid`/`rejected`). Открытой регистрации нет.
- серверный · миграция `015_partners_login.sql` — суррогатный `partners.id` (bigint identity) под `sessions.user_id`; PK остаётся `code`.
- серверный · роуты `/p` и `/p/*` → `p.html`; `css/*`,`js/*` отдаются и по префиксу `/p`. Пуши партнёру ведут на `/p`.
- дизайн-Claude · кабинет партнёра (`p.html` + `p.js`): вход, дашборд (переходы/заказы/выполнено/заработано, tier, баланс), QR + реф-ссылка, привязанные заказы, запрос вывода + история. Фронт всех ролей укомплектован.
- серверный · web-push заведён end-to-end: `src/push.js` (обёртка `web-push`, `setVapidDetails` из `.env`, `sendToSubscriber(type,id,{title,body,url})`; на 404/410 от push-сервиса → `push_subscriptions.disabled=true`, после 8 прочих ошибок подряд — тоже).
- серверный · воркер очереди в `server.js` (`setInterval` 15с): берёт `notifications` со `status='queued'`, рендерит по коду шаблона (`offer_received` → клиенту, ссылка `/z/<token>`; `order_assigned` / `order_cancelled` → мастеру, ссылка `/m/deals`), шлёт пуш, ставит `sent_at` + `status='sent'/'failed'` с текстом в `error`.
- серверный · `/sw.js` отдаётся с корня и с `Cache-Control: no-cache` (+ `Service-Worker-Allowed: /`); `manifest.json` и `icon-192/512.png` — статикой из `public/`.
- серверный · `public/js/push.js` (мой файл): `clientToken()` теперь берёт токен и из адреса `/z/<token>`, если в `localStorage` пусто — подписка клиента работает без правок фронта.
- дизайн-Claude · PWA-обвязка: `sw.js` (push + notificationclick по спеке серверного), `manifest.json`, иконки 192/512, `pwa.js` (установка на экран + кнопка «включить уведомления»), подключено в `index.html` и `m.html`.

## 2026-09-02

- серверный · блок 3b: `/api/z/*` для клиентской страницы — `GET /api/z/:token` (заявки + отклики + сделка + отзыв), `choose`/`cancel`/`review` с барьером «последние 4 цифры телефона». Выбор мастера: назначение, комиссия 30% + партнёрская, открытие контактов. E2E протестировано.
- серверный · роутинг `/z/<token>` и `/m/*` + отдача `css/*`,`js/*` по этим префиксам (index.html/m.html ссылаются относительными путями). Дизайн-Claude: добавьте `<base href="/">` в `<head>` — тогда эти костыли не нужны.
- серверный · блок 3a: эндпоинты кабинета мастера под контракт m.js — `/api/master/me` (плоский), `profile`, `photo`/`portfolio`/`documents` (multipart), `feed`, `deals`, `deals/:id/status` (трекер). Медиа: `/uploads/pub/*` публично, остальное под BasicAuth.
- серверный · блок 2: вход мастера только по инвайту (`/api/admin/masters/invite` → `/api/master/activate`), сессии (cookie `sid`, пароли на builtin scrypt), `@fastify/rate-limit`. Web-push каркас: `/api/push/vapid`, `/api/push/subscribe`, `public/js/push.js`.
- серверный · блок 1: миграции 004–014 — `leads`→`orders`, `clients` (токен на клиента, склейка по телефону), `masters`/`offers`/`deal_events`/`reviews`/`partners+`/`sessions`/`push_subscriptions`/`notifications`/`media`. Приём заявки создаёт клиента и выдаёт токен.
- дизайн-Claude · клиентская страница `/z` (js/z.js + экраны в index.html): список заявок, отклики мастеров, выбор с подтверждением 4 цифр, трекер сделки, отзыв.
- дизайн-Claude · собран фронт заявки (index.html): калькулятор по модулям, имя/телефон/район/адрес/дата-время/бюджет, загрузка фото, кнопка «Найти мастера», экран «Спасибо» со ссылкой на отклики.
- дизайн-Claude · прайс: кухня по модулям (2500 модульная / 3500 проектная за шкаф-полку), доп «врезка в столешницу» +1500, «вынос мусора» вместо «вывоз».
- серверный · приём заявки `POST /api/lead` → БД → уведомление владельцу в Telegram. Работает и протестировано E2E с формы.
- серверный · выдал схему БД и список эндпоинтов под полный продукт (вариант Б). Согласованы.
- дизайн-Claude · собран кабинет мастера (m.html + m.js): вход, активация по инвайту, анкета, лента заявок, отклик, сделки со статус-трекером.
- решение · мастера только по приглашению (инвайт от владельца), открытой регистрации нет.
- решение · вход клиента — браузерный токен + ссылка /z/<token>; уведомления — web-push (PWA), WhatsApp/SMS позже.
- решение · строим полный продукт (вариант Б), функции не урезаем.

## Как дописывать

Новые записи — сверху последней даты. Одна строка на изменение: `кто · что`. Кратко, по делу.

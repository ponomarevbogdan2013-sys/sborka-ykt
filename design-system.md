# Дизайн-система «СБОРКА»

Источник истины по внешнему виду. Эталон вживую — `reference/mockup.html`. Любой новый экран строится из этих токенов и компонентов, а не с нуля.

## Принципы

- Аудитория 40+, мобильный-first. Крупно, просто, минимум шагов.
- Бренд — тёмно-синий (цвет фирменной футболки мастеров), тёплый янтарь на том, что про деньги и действие.
- Живой мастер, реальные фото работ — не стоки и не мультяшки.
- Обе темы (светлая/тёмная) обязательны, через токены.

## Цвета (light)

| Токен | Hex | Назначение |
|---|---|---|
| `--navy` | #1b3a5b | бренд, заголовки, primary |
| `--navy2` | #274d75 | вторичный синий |
| `--amber` | #e0912f | акцент: цены, CTA |
| `--amber-ink` | #8a520d | текст на янтаре |
| `--green` | #1f9d63 | статус «выполнено», деньги |
| `--ink` | #14233a | основной текст |
| `--muted` | #5f7089 | вторичный текст |
| `--faint` | #8b9aae | подписи |
| `--bg` | #eef1f5 | фон приложения |
| `--card` | #ffffff | карточки |
| `--line` | #e4e9f0 | границы |

Тёмная тема — те же токены переопределяются в `@media (prefers-color-scheme:dark)` и `:root[data-theme="dark"]`. Значения — в `reference/mockup.html` (блок `:root`). Никогда не задавать цвет только внутри media/theme-блока.

## Типографика

- Шрифт: **Golos Text** (Google Fonts), веса 400/500/600/700/800. Родной для кириллицы.
- Fallback: `system-ui, sans-serif`.
- Заголовки экранов — 19px/800; крупные суммы — 26px/800 с `tabular-nums`; текст — 13.5–14px; подписи — 10.5–12px/600 с лёгким letter-spacing на капсе.

## Радиусы и тени

- Карты — 16px, кнопки — 13px, чипы/пилюли — 20px, плитки — 11–14px.
- `--shadow: 0 6px 24px rgba(20,35,58,.10)`, `--shadow-sm: 0 2px 8px rgba(20,35,58,.07)`.

## Компоненты (из макета)

- **Кнопки** `.btn`: `primary` (янтарь), `navy` (синяя), `ghost` (контурная). Радиус 13px, вес 700.
- **Карточка** `.card` — белая, граница `--line`, тень sm.
- **Чип** `.chip` — выбор из вариантов, активный — синий.
- **Бейдж статуса** — `new` (янтарь), `в работе` (синий фон), `выполнено` (зелёный фон).
- **Строка мастера** — аватар с инициалами, имя + бейдж «Проверен» (синий кружок с галочкой), звёзды, цена справа.
- **Статус-трекер** — 4 точки: назначен → едет → собирает → готово; done синий, active янтарный.
- **Калькулятор** — строки со степперами, допы чекбоксами, липкий итог с вилкой.
- **Плашки-подсказки** `.callout` — янтарная (внимание) или зелёная (успех).

## Иконки

Линейные SVG (stroke, без заливки), 1.6–2px. Не эмодзи в интерфейсе (эмодзи-плитки портфолио заменены на фото-плейсхолдеры). Портфолио и работы — реальные фото.

## Чек-лист перед публикацией экрана

- [ ] цвета только через токены, обе темы читаемы
- [ ] Golos Text подключён, кириллица не падает в fallback
- [ ] мобильная ширина, тап-цели ≥ 44px
- [ ] суммы с `tabular-nums`
- [ ] совпадает по стилю с `reference/mockup.html`
